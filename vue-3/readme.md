# 组件

组件是一个对象

## 两种类型

全局组件
局部组件

## 使用组件的方式

全局组件

```shell
// 组件的结构内容和实例相似，但是它需要注册才能使用
// name 是组件的命名名称，也是挂载元素
// option 是它的配置信息

// 创建一个实例
const vm=new Vue({
    el:'',
    data:{

    }
})
// 注册一个组件
Vue.component('name',{
    // 组件里也可以有和实例相同的选项
    // 首先是data 选项，主要是存放一些数据
    data(){

    },
    // 组件的模板必须啊放在template 选项里面
    template:``,
    // 处理函数，只能给组件自己使用
    methods:{

    }
    // 计算属性，只能给组件自己使用
    computed:{

    }

})
```
